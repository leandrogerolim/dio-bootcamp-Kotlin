
    fun main() {
        fun max(a: Int, b: Int) = if (a > b) a else b                     // 1 cria as duas variaveis A e B começa o
                                                                            // if se a é maior que B imprima o valor de a
                    //cria a função MAX como se tivesse um construtor inserido.    /// se não imprima o valor de b

        println(max(99, 1442))
    }
